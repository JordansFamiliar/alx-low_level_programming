#include "main.h"

/**
* reverse_array - reverse the elements in an array
* @a: the array
* @n: number of elements
*/

void reverse_array(int *a, int n)
{
	int *i = &a[n-1];

	int j = 0;

	for (n-1; n >= 0; n--)
	{
		i[j] = a[n];
		j++;
	}
	for (; n <= j; n++)
	{
		a[n] = i[n];
	}
}
