#include "main.h"

/**
* more_numbers - prints 0 - 14
*
* Return: void
*/
void more_numbers(void)
{
	int i, a, b;

	for (i = 1; i <= 10; i++)
	{
		for (a = 48; a <= 58; a++)
		{
			if (a >= 48 && a < 58)
				_putchar(a);
			if (a == 58)
			{
				for (b = 1
			}
		}
	}
}
