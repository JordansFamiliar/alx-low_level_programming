#include <stdio.h>

/**
* main - prints a string
* main takes no variables
*
* prints a string to the standard output
* Return: zero
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art, \n");
	return (0);
}
