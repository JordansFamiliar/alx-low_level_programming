#include <stdio.h>
#include <stdlib.h>

/**
* main - prints to stdout
*
* prints base16 to stdout
* Return: 0
*/
int main(void)
{
	/* define */
	char i;
	char a;

	/* init */
	i = '0';
	a = 'a';

	while (i <= 9)
	{
		putchar(i);
		i++;
	}
	while (a <= f)
	{
		putchar(a);
		a++;
	}
	putchar('\n');
	return (0);
}
